"use client";

import { Box } from "lucide-react";

export default function HackTheBoxIcon({ size = 24 }: { size?: number }) {
  return (
    <Box
      size={size}
      className="text-gray-700 dark:text-gray-300"
    />
  );
}

